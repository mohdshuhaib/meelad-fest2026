import "server-only";import ExcelJS from"exceljs";
export async function workbookResponse(title:string,columns:{header:string;key:string;width:number}[],rows:Record<string,unknown>[],filename:string){const wb=new ExcelJS.Workbook();wb.creator="Ahlu Saada Meelad Fest";wb.created=new Date();const ws=wb.addWorksheet(title.slice(0,31),{views:[{state:"frozen",ySplit:3}]});ws.mergeCells(1,1,1,columns.length);const heading=ws.getCell(1,1);heading.value="AHLU SAADA MEELAD FEST";heading.font={bold:true,size:18,color:{argb:"FFFFFFFF"}};heading.fill={type:"pattern",pattern:"solid",fgColor:{argb:"FF074A40"}};heading.alignment={vertical:"middle"};ws.getRow(1).height=34;ws.mergeCells(2,1,2,columns.length);ws.getCell(2,1).value=`${title} | Generated ${new Intl.DateTimeFormat("en-IN",{dateStyle:"medium",timeStyle:"short",timeZone:"Asia/Kolkata"}).format(new Date())} IST | ${rows.length} records`;ws.getCell(2,1).font={italic:true,color:{argb:"FF64706D"}};ws.columns=columns;const header=ws.getRow(3);columns.forEach((c,i)=>{const cell=header.getCell(i+1);cell.value=c.header;cell.font={bold:true,color:{argb:"FFFFFFFF"}};cell.fill={type:"pattern",pattern:"solid",fgColor:{argb:"FFD0A95B"}};cell.alignment={wrapText:true}});for(const row of rows)ws.addRow(row);ws.eachRow((row,n)=>{if(n>3&&n%2===0)row.eachCell(cell=>cell.fill={type:"pattern",pattern:"solid",fgColor:{argb:"FFF5F0E4"}});row.eachCell(cell=>{cell.alignment={vertical:"top",wrapText:true};cell.border={bottom:{style:"hair",color:{argb:"FFDDDDDD"}}}})});ws.autoFilter={from:{row:3,column:1},to:{row:Math.max(3,rows.length+3),column:columns.length}};const buffer=await wb.xlsx.writeBuffer();return new Response(Buffer.from(buffer),{headers:{"content-type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","content-disposition":`attachment; filename="${filename}"`,"cache-control":"no-store"}})}
