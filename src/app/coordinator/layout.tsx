import { requireCoordinator } from "@/lib/staff-auth"; import { CoordinatorNav } from "./coordinator-nav";
export default async function Layout({children}:{children:React.ReactNode}){const {profile}=await requireCoordinator();return <div className="min-h-screen bg-[#f3f5f2]"><CoordinatorNav name={profile.name}/><div className="lg:pl-64"><header className="flex h-16 items-center border-b bg-white px-5 lg:px-8"><p className="ml-12 text-xs font-bold tracking-[.2em] text-emerald lg:ml-0">COORDINATOR PANEL</p></header><main className="mx-auto max-w-6xl p-4 sm:p-6 lg:p-8">{children}</main></div></div>}
