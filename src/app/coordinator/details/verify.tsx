"use client";import {useState} from "react";import {useRouter} from "next/navigation";export function CoordinatorVerify({id}:{id:string}){const router=useRouter(),[status,setStatus]=useState("verified"),[reason,setReason]=useState("");async function save(){const r=await fetch(`/api/coordinator/status/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status,reason})});if(r.ok)router.refresh()}return <div className="mt-3 grid gap-2 sm:grid-cols-[180px_1fr_auto]"><select value={status} onChange={e=>setStatus(e.target.value)} className="h-10 rounded-lg border px-2"><option value="verified">Verify</option><option value="rejected">Reject</option><option value="resubmission_required">Request resubmission</option></select><input value={reason} onChange={e=>setReason(e.target.value)} placeholder="Reason / coordinator note" className="h-10 rounded-lg border px-2"/><button onClick={save} className="rounded-lg bg-emerald px-4 text-xs font-bold text-white">Save</button></div>}
