import { redirect } from "next/navigation"; import { getStaff } from "@/lib/staff-auth"; import { ChangePasswordForm } from "./change-password-form";
export default async function ChangePassword(){const staff=await getStaff();if(!staff)redirect("/staff/login");return <main className="grid min-h-screen place-items-center bg-emerald p-4"><div className="w-full max-w-md rounded-[2rem] bg-white p-7 shadow-xl sm:p-9"><p className="text-xs font-bold uppercase tracking-[.22em] text-gold">Account security</p><h1 className="mt-2 font-serif text-3xl font-semibold">Change your password</h1><p className="mt-3 text-sm leading-6 text-muted">Temporary passwords must be replaced before staff tools become available. Your new password only needs at least 6 characters.</p><ChangePasswordForm/></div></main>}
