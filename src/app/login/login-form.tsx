"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { LoaderCircle } from "lucide-react";
export function LoginForm({registrationId}:{registrationId:string}){const router=useRouter();const [pending,setPending]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError("");const form=new FormData(e.currentTarget);const res=await fetch("/api/auth/participant/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});setPending(false);if(!res.ok){setError("INVALID REGISTRATION ID OR ACCESS CODE.");return}router.replace("/participant");router.refresh()}return <form onSubmit={submit} className="mt-7 space-y-5"><label className="text-sm font-bold">Registration ID<input name="registrationId" defaultValue={registrationId} required onInput={e=>e.currentTarget.value=e.currentTarget.value.toUpperCase()} autoCapitalize="characters" autoComplete="username" className="mt-2 h-12 w-full rounded-xl border border-ink/15 px-4 uppercase" placeholder="RFMJXXXXXXXX"/></label><label className="block text-sm font-bold">Six-digit access code<input name="accessCode" required type="password" inputMode="numeric" pattern="[0-9]{6}" maxLength={6} autoComplete="current-password" className="mt-2 h-12 w-full rounded-xl border border-ink/15 px-4 tracking-[.35em]" placeholder="••••••"/></label>{error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-center text-xs font-bold text-red-700">{error}</p>}<button disabled={pending} className="flex min-h-12 w-full items-center justify-center gap-2 rounded-full bg-emerald font-bold text-white disabled:opacity-60">{pending&&<LoaderCircle className="animate-spin" size={18}/>}Continue securely</button></form>}
