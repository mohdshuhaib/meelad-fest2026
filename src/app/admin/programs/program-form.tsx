"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { LoaderCircle, Plus } from "lucide-react";
export function ProgramForm(){const router=useRouter();const [open,setOpen]=useState(false);const [pending,setPending]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError("");const body=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/admin/programs",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const data=await res.json();setPending(false);if(!res.ok){setError(data.message);return}setOpen(false);router.refresh()}if(!open)return <button onClick={()=>setOpen(true)} className="mt-6 flex min-h-11 items-center gap-2 rounded-full bg-emerald px-5 text-sm font-bold text-white"><Plus size={17}/>Add Programme</button>;return <form onSubmit={submit} className="mt-6 rounded-2xl border border-emerald/20 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-serif text-xl font-semibold">Add programme</h2><button type="button" onClick={()=>setOpen(false)} className="text-sm font-bold text-muted">Cancel</button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field name="code" label="Code"/><Field name="name" label="Programme name"/><Select name="category_eligibility" label="Category" values={["junior","senior","super_senior","general"]}/><Select name="gender_eligibility" label="Gender" values={["male","female","general"]}/><Field name="submission_form_url" label="Google Form URL" type="url"/><Select name="global_status" label="Global status" values={["not_started","ongoing","closed"]}/><Field name="registration_id_entry_key" label="Registration ID entry key"/><Field name="full_name_entry_key" label="Full Name entry key"/><label className="text-sm font-bold sm:col-span-2">Description<textarea name="description" className="mt-2 min-h-24 w-full rounded-xl border border-ink/15 p-3"/></label></div>{error&&<p role="alert" className="mt-4 text-sm font-semibold text-red-700">{error}</p>}<button disabled={pending} className="mt-5 flex min-h-11 items-center gap-2 rounded-full bg-emerald px-6 text-sm font-bold text-white disabled:opacity-50">{pending&&<LoaderCircle className="animate-spin" size={17}/>}Create programme</button></form>}
function Field({name,label,type="text"}:{name:string;label:string;type?:string}){return <label className="text-sm font-bold">{label}<input required={name==="code"||name==="name"} name={name} type={type} className="mt-2 h-11 w-full rounded-xl border border-ink/15 px-3 uppercase"/></label>};function Select({name,label,values}:{name:string;label:string;values:string[]}){return <label className="text-sm font-bold">{label}<select name={name} className="mt-2 h-11 w-full rounded-xl border border-ink/15 px-3">{values.map(v=><option key={v} value={v}>{v.replaceAll("_"," ").toUpperCase()}</option>)}</select></label>}
