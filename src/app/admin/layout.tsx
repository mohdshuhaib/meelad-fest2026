import { requireAdmin } from "@/lib/staff-auth"; import { AdminNav } from "./admin-nav";
export default async function AdminLayout({children}:{children:React.ReactNode}){const {profile}=await requireAdmin();return <div className="min-h-screen bg-[#f3f5f2] text-ink"><AdminNav name={profile.name}/><div className="lg:pl-64"><header className="sticky top-0 z-30 flex h-16 items-center border-b border-ink/8 bg-white/90 px-5 backdrop-blur lg:px-8"><p className="ml-12 text-xs font-bold tracking-[.22em] text-emerald lg:ml-0">ADMIN PANEL</p><span className="ml-auto text-sm font-semibold text-muted">{profile.name}</span></header><main className="mx-auto max-w-7xl p-4 sm:p-6 lg:p-8">{children}</main></div></div>}
