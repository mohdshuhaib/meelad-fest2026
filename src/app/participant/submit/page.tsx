import { MessageCircle, Users } from "lucide-react"; import { requireParticipant } from "@/lib/participant-session"; import { createAdminClient } from "@/lib/supabase/admin"; import { SubmissionCard } from "./submission-card";
export default async function Submit(){const p=await requireParticipant();const group=Array.isArray(p.groups)?p.groups[0]:p.groups;const coordinator=Array.isArray(p.profiles)?p.profiles[0]:p.profiles;const {data:selections=[]}=await createAdminClient().from("participant_programs").select("id,program_id,participant_progress_status,verification_status,form_opened_at,rejection_reason,programs(code,name,global_status,submission_form_url)").eq("participant_id",p.id).order("selected_at");const message=encodeURIComponent(`ASSALAMU ALAIKUM. I AM ${p.name}, REGISTRATION ID ${p.registration_id}. I NEED ASSISTANCE REGARDING THE AHLU SAADA MEELAD FEST.`);return <div><p className="text-xs font-bold uppercase tracking-[.22em] text-gold">Submissions</p><h1 className="mt-2 font-serif text-3xl font-semibold">Submit your entries</h1><div className="mt-6 grid gap-4 sm:grid-cols-2"><section className="rounded-2xl bg-white p-5 shadow-sm"><Users className="text-emerald"/><h2 className="mt-4 font-serif text-xl font-semibold">Your group</h2>{group?<><p className="mt-1 text-sm text-muted">{group.name}</p>{group.whatsapp_group_link&&<a href={group.whatsapp_group_link} target="_blank" rel="noreferrer" className="mt-4 flex min-h-11 items-center justify-center gap-2 rounded-full bg-[#1fa855] px-4 text-xs font-bold text-white"><MessageCircle size={17}/>JOIN WHATSAPP GROUP</a>}</>:<p className="mt-3 text-xs font-bold leading-5 text-muted">YOUR GROUP HAS NOT YET BEEN ASSIGNED. PLEASE CHECK AGAIN LATER.</p>}</section><section className="rounded-2xl bg-white p-5 shadow-sm"><MessageCircle className="text-emerald"/><h2 className="mt-4 font-serif text-xl font-semibold">Coordinator</h2>{coordinator?<><p className="mt-1 text-sm text-muted">{coordinator.name} · {coordinator.whatsapp_number}</p><a href={`https://wa.me/91${coordinator.whatsapp_number}?text=${message}`} target="_blank" rel="noreferrer" className="mt-4 flex min-h-11 items-center justify-center rounded-full border border-emerald px-4 text-xs font-bold text-emerald">MESSAGE COORDINATOR</a></>:<p className="mt-3 text-sm text-muted">A coordinator has not been assigned yet.</p>}</section></div><h2 className="mt-9 text-center font-serif text-2xl font-semibold">Selected programme submissions</h2><div className="mt-5 space-y-4">{selections?.map(s=>{const program=Array.isArray(s.programs)?s.programs[0]:s.programs;return <SubmissionCard key={s.id} selection={{...s,program}}/>})}{!selections?.length&&<p className="rounded-2xl border border-dashed p-8 text-center text-sm text-muted">Select a programme before beginning a submission.</p>}</div></div>}
