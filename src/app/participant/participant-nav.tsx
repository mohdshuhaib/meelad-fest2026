"use client"; import Link from "next/link"; import { usePathname } from "next/navigation"; import { Home, ListChecks, Send } from "lucide-react";
const links=[{href:"/participant",label:"HOME",icon:Home},{href:"/participant/programs",label:"PROGRAMS",icon:ListChecks},{href:"/participant/submit",label:"SUBMIT",icon:Send}];
export function ParticipantNav(){const path=usePathname();return <nav aria-label="Participant navigation" className="fixed inset-x-0 bottom-0 z-40 border-t border-ink/10 bg-white/95 px-2 pb-[max(.5rem,env(safe-area-inset-bottom))] pt-2 shadow-[0_-8px_28px_rgba(0,0,0,.08)] backdrop-blur md:sticky md:top-8 md:h-fit md:rounded-2xl md:border md:p-2 md:shadow-sm"><div className="mx-auto grid max-w-md grid-cols-3 gap-1 md:grid-cols-1">{links.map(({href,label,icon:Icon})=>{const active=href==="/participant"?path===href:path.startsWith(href);return <Link key={href} href={href} className={`flex min-h-15 flex-col items-center justify-center gap-1 rounded-xl text-[10px] font-bold tracking-wide transition md:min-h-12 md:flex-row md:justify-start md:px-4 md:text-xs ${active?"bg-emerald text-white":"text-muted hover:bg-emerald/5 hover:text-emerald"}`}><Icon size={20}/>{label}</Link>})}</div></nav>}
